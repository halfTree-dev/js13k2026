JS13K2026 gamejam 主题：独角兽和彩虹

# 基本设定
世界的颜色被一个未知的怪物夺走，守护颜色的独角兽因此苏醒，它必须与各类异常战斗，将各类色彩重新夺回。

# 基本玩法框架
游戏类型：横版跑酷

游戏流程：玩家操控独角兽，在黑白城市场景中奔跑，躲避障碍，并与带有各类颜色的怪物战斗，将色彩重新夺回。
过程中，黑白场景自身会后退，营造出独角兽在不断奔跑的感觉；

一个场景由占据屏幕下方一小部分的黑色地面，和上方大部分的背景黑白剪影构成。

独角兽具有一个耐力条，分为三段，会缓慢恢复。

基本操作：
- A/D 独角兽加速/减速，令其在屏幕中左右调整位置；
- W 跳跃，支持二段跳；
- S 加速下坠，在空中时独角兽可以加速下坠；
- J 从独角兽的角处发射弹幕；

# 游戏大致流程

## 启动演出

启动游戏时，游戏先是整个屏幕处于黑暗，接着以下的文本从屏幕中间浮现：

```txt
Woke up from the biting cold

Realized the world lost its color
```

接着，随着一阵屏幕闪光，屏幕的上半部分从黑暗遮罩中解除，露出上半部分的黑白城市剪影，以下的文字从屏幕中下部分浮现

```txt
As a unicorn

It's time to take responsibility.
```

接着，随着一阵屏幕闪光，整个屏幕从黑色遮罩中解除。展示正常游戏画面，独角兽位于屏幕中央，在地面上奔跑。

暂时不显示玩家的生命和颜色 UI，显示游戏的标题 `Colorless City` 位于屏幕正中央偏上，显示提示 `Press any key to start taking back colors`

当玩家在游戏开始前按下任意键时，游戏开始，标题和提示消失，游戏正式开始。

## 教程阶段

通过检测玩家是否首次开始游戏（不需要 localStorage，仅需要判定是否在当前页面中首次启动），来判断是否进入教程。

进入教程后，以下文本将会保持将近 5 秒：

位于屏幕正中央偏上：
```txt
[KeyW] to jump
```

位于屏幕正中央偏下：
```txt
[KeyS] to accelerate falling
```

位于屏幕偏左：
```txt
[KeyA] to move left
```

位于屏幕偏右：
```txt
[KeyD] to move right
```

接下来上述文本消失，以下文本将会保持将近 5 秒：

位于生命条上方：
```txt
This shows your hit points
```

位于颜色条上方：
```txt
This shows your progress in taking back colors
```

接下来上述文本消失，以下文本将会保持将近 20 秒
```txt
Avoid black obstacles!
```

在这段话出现 5 秒后，一系列黑色的障碍物会随机出现在屏幕右侧，向左移动。总共有 3 个，每个间隔 5 秒。

接下来上述文本消失，以下文本将会保持将近 20 秒
```txt
[KeyJ] to shoot
```

在这段话出现 5 秒后，一系列仅向左移动的黑色的敌怪会出现在屏幕右侧，向左移动。总共有 3 个，每个间隔 5 秒。

这些流程都结束后，教程阶段结束，同时“玩家已经通过教程”标记被设置。
该标记仅存储在脚本中，不固化在本地，若玩家完全退出游戏，则会重新进入教程。

## 游戏进行：收集色彩——红色

该阶段开始时，屏幕上方会出现逐次以上的红色文本，然后消失
```txt
Well, well, well

Look at our savior

You really think you can take back the colors?
```

**这些障碍物会出现在本关卡**

- 黑色广告牌
- 黑色交通牌

- 黑色路灯
- 黑色路障

- 黑色出租车
- 黑色电线杆
- 黑色警示护栏

- 黑色白杨树
- 黑色灌木丛
- 黑色垃圾桶组

- 黑色共享单车

所有的障碍物外表都是和地面一致的纯黑色，体现黑色会夺走颜色的感觉。

近景存在这些物体处于灰色/正常颜色的版本。

所有的障碍物都可以被看作是没有发射弹幕功能，生命值为 999，拥有矩形碰撞箱，默认行为为向左匀速移动的 entity，其匀速移动速度和世界滚动速度一致。

**这些敌怪会出现在本关卡**

- 黑色警卫无人机

大小近似半个独角兽，呈直线从右向左移动，每隔 10 秒向玩家发射加速射弹；

- 黑色大型蜘蛛

大小近似半个独角兽，从右向左跳动，行动轨迹类似绝对值正弦函数，不会发射弹幕；

接下来，以下逻辑开始执行：

每隔 5 秒，以下生成动作中的一种就会随机被触发：

1. 接下来，每隔 1 秒从障碍物列表中随机抽取一个，进行生成，持续 5 次。

2. 接下来，从敌怪生成规则组中抽取一个，进行生成，持续 5 次。

规则组：
- 在屏幕 1/2 高度到 1/4 高度之间随机抽取一个高度，随机生成一排水平无人机；
- 在屏幕 1/2 高度到 1/4 高度之间随机抽取一个高度，随机生成一纵列垂直无人机；
- 在屏幕最右端生成一只蜘蛛，蜘蛛的相位随机；


颜色条会以 1/450 每秒的速度极度缓慢增加。此外，每摧毁一个敌人都将获得 1/40 的颜色条。

直到颜色条到达 1，屏幕发生一阵闪光，障碍物和敌怪都消失，进入下一个阶段。

这个阶段过后，近景以红色为主题色的建筑物会恢复其本来颜色。

## 游戏进行：收集色彩——橙色